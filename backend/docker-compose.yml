services:
  postgres:
    image: postgres:17.2-alpine3.20
    environment:
      POSTGRES_DB: "cars_db"
      POSTGRES_USER: "imbalet"
      POSTGRES_PASSWORD: "pswd123"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - ../2. Init Database:/docker-entrypoint-initdb.d
      - habrdb-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  habrdb-data:
